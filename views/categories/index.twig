{% extends "layout/default/template.twig" %}
{% block title %}{{title}}{% endblock %}

{% block content %}

<div class="card">
    <div class="card-body">
        <h1 class="card-title">
            {{asunto}} 
            <a href="{{BASE}}categories/create" class="btn btn-outline-secondary">Nueva Categoria</a>
        </h1>
        {% include 'partials/_messages.twig' %}
        {% if categoria %}
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>categoria</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    {% for model in categoria %}
                        <tr>
                            <td>{{model.id}}</td>
                            <td>{{model.nombre}}</td>
                            <td>
                                <a href="{{BASE}}categories/show/{{model.id}}"
                                    class="btn btn-success btn-sm">Ver</a>
                                <a href="{{BASE}}categories/edit/{{model.id}}" 
                                    class="btn btn-warning btn-sm">Editar</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p class="text-info">{{mensaje}}</p>
        {% endif %}
    </div>
</div>

{% endblock %}